import { INavigation } from '@interfaces/common';
import Portfolio from '@pages/Portfolio';

const MasterPortfolio = (props: INavigation) => {
    return <Portfolio {...props} />
}

export default MasterPortfolio